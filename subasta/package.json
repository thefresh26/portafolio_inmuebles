{
  "name": "subasta-activa",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:server": "pnpm --filter @subasta/shared build && pnpm --filter @subasta/server dev",
    "dev:web": "pnpm --filter @subasta/shared build && pnpm --filter @subasta/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "smoke-test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
